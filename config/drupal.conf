# Drupal JSON:API Configuration
# This file contains connection details for exporting to Drupal

# Base URL of the Drupal site (no trailing slash)
base_url=https://audits.frontier-cnib.ca

# HTTP Basic Authentication credentials
username=claude
password=venez1a?

# Enable/disable Drupal export functionality
enabled=true
